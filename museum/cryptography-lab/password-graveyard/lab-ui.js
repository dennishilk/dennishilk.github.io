document.addEventListener('DOMContentLoaded',()=>{const C=window.PasswordCore,$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];$('[data-reset]').onclick=()=>location.reload();$$('[data-practice]').forEach(b=>b.onclick=()=>b.classList.toggle('buried'));$('#leak').onclick=()=>$('#leak-result').textContent='DATABASE LEAK: every fictional password is exposed as readable text.';function hashes(){let u=C.stored('sunshine'),a=C.stored('sunshine','ALICE-SALT'),b=C.stored('sunshine','BOB-SALT');$('#hash-result').textContent=`unsalted: ${u} = ${u}; salted: ${a} ≠ ${b}. Tiny lookup “sunshine” → ${C.rainbow('sunshine')||'toy digest'} cannot universally reuse a salt.`}$('#salt').onclick=hashes;hashes();const cost=$('#cost');function work(){let w=C.work(+cost.value);$('#work-result').textContent=`cost ${cost.value}: one login ≈ ${w.login} units; mass guessing ≈ ${w.mass} units.`}cost.oninput=work;work();$('#reuse').onclick=()=>$('#reuse-result').textContent='FORUM leak → reused secret puts MAIL and SHOP at risk. No services are contacted.'});
